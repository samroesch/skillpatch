# compiled binaries (analytics service is now Node — no build step)

# runtime
broker-plugin/cache/
storage/events.log

# legacy / replaced
broker-plugin/hooks/prompt_broker.py
storage/skills.json

# dev artifacts
*.log

# internal planning files — never publish
PLAN.md
The idea.html
The idea_files/
